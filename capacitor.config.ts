import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.alitajs.sfg',
  appName: 'sfg',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
